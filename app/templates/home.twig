{% extends 'base.twig' %}
{% block title %}Home{% endblock %}
{% block body %}
    <body id="page-top" data-spy="scroll" data-target=".navbar-fixed-top" onload="setTimeout('location.reload()',900000)">
        <!--  -->
        {# Navigation #}
        {% include 'section/navigation.twig' %}

        

        {% if admin %}
            {# Admin Section #}
            {% include 'section/admin.twig' %}
            
        {% else %}
            {# time #}
            {% include 'section/time.twig' %}

            {# Buying Pricelist #}
            {% include 'section/buylist.twig' %}

            {# product Prolist #}
            {% include 'section/productlist.twig' %}

            {# product Selllist #}
            {% include 'section/selllist.twig' %}

            

        {% endif %}

        {% include 'section/display.twig' %}


    </body>

    {% include 'section/footer.twig' %}
    
{% endblock %}

{% block js %}
    <script src="js/main.home.js"></script>
    
{% endblock %}